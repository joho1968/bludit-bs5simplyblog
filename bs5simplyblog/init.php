<?php
defined( 'BLUDIT' ) || die( 'That did not work as expected.' );

// Let's not do this, since the BS5SimplyBlog Plugin is optional :-)
/*
if ( $themePlugin == false ) {
  exit( 'To ensure proper functionality, the theme requires the BS5SimplyBlog plugin. ' .
        'Activate the plugin through the admin panel.' );
}
*/
